import { register } from 'swiper/element/bundle';
import { slides } from '../../constants/data';



register();

const Slider = () => {
   return (
      <div>Slider</div>
   )
};

export default Slider;
